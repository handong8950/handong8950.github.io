# 企业网络安全防护体系建设：一步一步做

## 0. 先说结论

这份 PDF 要你做的不是一个小实验，而是把前面学过的内容组合成一个综合项目：

1. 画出 PNET 总拓扑。
2. 规划 IP 地址、VLAN、安全域。
3. 配置新校区：VLAN、Trunk、单臂路由、DHCP、NAT、安全策略。
4. 发布 IDC_Server 的 HTTP 服务，让外网通过防火墙公网 IP 的 80 端口访问。
5. 配置新校区和丛台校区之间的 IPSec VPN。
6. 最后按验收标准截图：接口、DHCP、ping、NAT/策略、端口映射、VPN 状态。

PDF 里的 HA 不用配置。后面的端口扫描、病毒过滤、DoS、注入攻击更像扩展攻防演练，先把前 3 个需求做通。

---

## 1. 推荐 IP 地址规划

你的图里已经写了类似 `192.168.2.0/24`、`10.10.2.0/24`，我按这个思路整理成一套完整规划。后面所有配置都按这张表来。

| 区域 | 设备/接口 | IP/网段 | 网关 | 说明 |
|---|---|---:|---:|---|
| 新校区教职工 VLAN10 | Core_Firewall01 e0/1.10 | 192.168.2.1/24 | - | Staff 网关 |
| 新校区教职工终端 | Staff_PC | DHCP，建议池 192.168.2.100-192.168.2.200 | 192.168.2.1 | VLAN 10 |
| 新校区学生 VLAN20 | Core_Firewall01 e0/1.20 | 10.10.2.1/24 | - | Student 网关 |
| 新校区学生终端 | Student_PC | DHCP，建议池 10.10.2.100-10.10.2.200 | 10.10.2.1 | VLAN 20 |
| 新校区 IDC | Core_Firewall01 e0/2 | 10.60.60.1/24 | - | 服务器区网关 |
| 新校区服务器 | IDC_Server | 10.60.60.10/24 | 10.60.60.1 | HTTP 服务 |
| 新校区公网出口 | Core_Firewall01 e0/4 | 202.100.2.2/24 | 202.100.2.1 | 出口公网 IP |
| Internet 路由器到新校区 | Router e0/1 | 202.100.2.1/24 | - | 模拟公网 |
| Internet 路由器到 Guest | Router e0/0 | 202.100.100.1/24 | - | 模拟公网用户网关 |
| 外网用户 | Guest_User | 202.100.100.10/24 | 202.100.100.1 | 访问公网发布服务 |
| Internet 路由器到丛台 | Router e0/2 | 172.16.2.1/24 | - | 模拟丛台公网 |
| 丛台防火墙公网口 | Branch_Firewall e0/4 | 172.16.2.2/24 | 172.16.2.1 | VPN 对端公网 IP |
| 丛台内网 | Branch_Firewall e0/1 | 192.168.30.1/24 | - | Branch_User 网关 |
| 丛台终端 | Branch_User | DHCP，建议池 192.168.30.100-192.168.30.200 | 192.168.30.1 | 丛台内网 |

DNS 可以统一填 `8.8.8.8`，如果实验环境不能联网，也可以填 Internet 路由器或老师指定 DNS。

---

## 2. PNET 拓扑应该长什么样

你现在的图大体是对的。最终应包含这些节点：

| 节点 | 作用 |
|---|---|
| Core_Firewall01 | 新校区核心防火墙 |
| Switch | 新校区接入交换机 |
| Staff_PC | 教职工终端，VLAN 10 |
| Student_PC | 学生终端，VLAN 20 |
| IDC_Server | 新校区服务器，开 HTTP |
| Router | 模拟 Internet |
| Guest_User | 外网公网用户 |
| Branch_Firewall | 丛台校区防火墙 |
| Branch_User | 丛台校区内网终端 |

连线按下面来：

| 连接 | 用途 |
|---|---|
| Staff_PC -> Switch e0/1 | VLAN 10 Access |
| Student_PC -> Switch e0/2 | VLAN 20 Access |
| Switch e0/0 -> Core_Firewall01 e0/1 | Trunk，放行 VLAN 10、20 |
| IDC_Server -> Core_Firewall01 e0/2 | IDC 服务器区 |
| Core_Firewall01 e0/4 -> Router e0/1 | 新校区公网出口 |
| Guest_User -> Router e0/0 | 外网用户 |
| Branch_Firewall e0/4 -> Router e0/2 | 丛台公网出口 |
| Branch_User -> Branch_Firewall e0/1 | 丛台内网 |

---

## 3. 第一步：交换机配置 VLAN 和 Trunk

进入 Switch 终端，粘贴：

```text
enable
configure terminal

vlan 10
 name Staff
exit
vlan 20
 name Student
exit

interface Ethernet0/1
 switchport mode access
 switchport access vlan 10
 no shutdown
exit

interface Ethernet0/2
 switchport mode access
 switchport access vlan 20
 no shutdown
exit

interface Ethernet0/0
 switchport trunk encapsulation dot1q
 switchport mode trunk
 switchport trunk allowed vlan 10,20
 no shutdown
exit

end
write memory
```

如果你的交换机不支持 `switchport trunk encapsulation dot1q`，报错就跳过这一行，继续后面的命令。

验证命令：

```text
show vlan brief
show interfaces trunk
```

要截图：VLAN 10、VLAN 20 存在；Trunk 口允许 VLAN 10、20。

---

## 4. 第二步：Internet Router 配置公网三段

进入 Router 终端，按实际接口名调整。若你的接口是 `GigabitEthernet0/0`，就把下面的 `Ethernet0/0` 替换掉。

```text
enable
configure terminal

interface Ethernet0/0
 ip address 202.100.100.1 255.255.255.0
 no shutdown
exit

interface Ethernet0/1
 ip address 202.100.2.1 255.255.255.0
 no shutdown
exit

interface Ethernet0/2
 ip address 172.16.2.1 255.255.255.0
 no shutdown
exit

end
write memory
```

验证：

```text
show ip interface brief
```

---

## 5. 第三步：Core_Firewall01 基础接口和安全域

防火墙建议用 Web 界面做，比 CLI 不容易输错。

登录账号如果是 Hillstone：

```text
hillstone / hillstone
```

首次强制改密时统一改成：

```text
Training@123
```

### 5.1 创建安全域

创建这些 Zone：

| Zone 名称 | 放哪个接口 |
|---|---|
| Internet | e0/4 |
| Office-Staff | e0/1.10 |
| Office-Student | e0/1.20 |
| IDC | e0/2 |
| VPN/Branch | VPN 隧道接口 |

如果界面要求用 trust/untrust/dmz，也可以这样对应：

| PDF 区域 | 可对应 Zone |
|---|---|
| Internet | untrust |
| Office-Staff | trust 或 Office-Staff |
| Office-Student | trust 或 Office-Student |
| IDC | dmz |
| Branch/VPN | vpn |

### 5.2 配接口

| 接口 | 类型 | VLAN | IP |
|---|---|---:|---:|
| e0/1 | 物理 Trunk 承载口 | - | 不配 IP，开启 |
| e0/1.10 | 子接口 | 10 | 192.168.2.1/24 |
| e0/1.20 | 子接口 | 20 | 10.10.2.1/24 |
| e0/2 | 三层接口 | - | 10.60.60.1/24 |
| e0/4 | 三层接口 | - | 202.100.2.2/24 |

重点：

1. e0/1.10 要封装 VLAN ID 10。
2. e0/1.20 要封装 VLAN ID 20。
3. 所有接口都要启用。
4. 所有接口都开启 `ping/manage ping`，因为 PDF 验收要 ping 网关。
5. 默认路由指向 Internet Router：

```text
0.0.0.0/0 -> 202.100.2.1
```

验证：Staff_PC 能 ping `192.168.2.1`，Student_PC 能 ping `10.10.2.1`，IDC_Server 能 ping `10.60.60.1`。

---

## 6. 第四步：DHCP

在 Core_Firewall01 上建两个 DHCP 地址池：

| 地址池 | 绑定接口 | 地址范围 | 网关 | DNS |
|---|---|---|---|---|
| DHCP_Staff | e0/1.10 | 192.168.2.100-192.168.2.200 | 192.168.2.1 | 8.8.8.8 |
| DHCP_Student | e0/1.20 | 10.10.2.100-10.10.2.200 | 10.10.2.1 | 8.8.8.8 |

Staff_PC 和 Student_PC 改成 DHCP 获取地址。

验收截图：

1. Staff_PC 获取到 192.168.2.x。
2. Student_PC 获取到 10.10.2.x。
3. 两台 PC 都能 ping 自己网关。

---

## 7. 第五步：IDC_Server 配置

IDC_Server 配静态 IP：

```text
IP: 10.60.60.10
Mask: 255.255.255.0
Gateway: 10.60.60.1
DNS: 8.8.8.8
```

如果是 Linux 服务器，确认 HTTP 服务开着。常见命令：

```text
ip addr
ip route
curl http://127.0.0.1
```

如果没有 Web 服务，可以临时启动：

```text
python3 -m http.server 80
```

需要 root 权限时：

```text
sudo python3 -m http.server 80
```

---

## 8. 第六步：基础安全策略

策略顺序很重要，从上到下匹配。建议按这个顺序建：

| 顺序 | 策略名 | 源 Zone | 目的 Zone | 服务 | 动作 | 日志 |
|---:|---|---|---|---|---|---|
| 1 | Staff_to_IDC_HTTP | Office-Staff | IDC | HTTP、ICMP | Permit | 开 |
| 2 | Staff_to_Internet | Office-Staff | Internet | any | Permit | 开 |
| 3 | Student_to_Internet | Office-Student | Internet | any | Permit | 开 |
| 4 | IDC_to_Internet | IDC | Internet | any | Permit | 开 |
| 5 | Internet_to_IDC_HTTP | Internet | IDC | HTTP | Permit | 开 |
| 6 | Branch_to_NewCampus | VPN/Branch | Office-Staff、Office-Student | ICMP | Permit | 开 |
| 7 | Student_to_Staff_Deny | Office-Student | Office-Staff | any | Deny | 开 |
| 8 | Deny_All | any | any | any | Deny | 开 |

先为了实验通过，可以把服务放宽为 `any`。截图时重点能说明：允许的能通，不允许的不通。

验收：

1. Staff_PC ping IDC_Server：通。
2. Staff_PC 访问 IDC_Server HTTP：通。
3. Student_PC ping Staff_PC：不通。
4. Staff_PC、Student_PC、IDC_Server ping Guest_User：通。

---

## 9. 第七步：源 NAT，让内网能访问公网

在 Core_Firewall01 配 SNAT：

| 项 | 内容 |
|---|---|
| 源 Zone | Office-Staff、Office-Student、IDC |
| 目的 Zone | Internet |
| 源地址 | 192.168.2.0/24、10.10.2.0/24、10.60.60.0/24 |
| 目的地址 | any |
| 转换方式 | Easy IP / 出接口地址 |
| 出接口 | e0/4 |
| NAT 后地址 | 使用 e0/4 地址 202.100.2.2 |

验收：

```text
Staff_PC ping 202.100.100.10
Student_PC ping 202.100.100.10
IDC_Server ping 202.100.100.10
```

防火墙截图：NAT 规则、会话表/日志里能看到源地址被转换成 `202.100.2.2`。

---

## 10. 第八步：端口映射发布选课系统

需求二要求：外网和内网用户都能通过 Core_Firewall01 的 e0/4 地址 `202.100.2.2:80` 访问 IDC_Server 的 HTTP。

在 Core_Firewall01 配 NAT Server / DNAT：

| 项 | 内容 |
|---|---|
| 公网地址 | 202.100.2.2 |
| 公网端口 | TCP 80 |
| 内网服务器 | 10.60.60.10 |
| 内网端口 | TCP 80 |
| 服务 | HTTP |

还要有安全策略：

```text
Internet -> IDC 允许 HTTP
Office-Staff -> IDC 允许 HTTP
Office-Student -> IDC 允许 HTTP
```

验收：

```text
Guest_User 访问 http://202.100.2.2
Staff_PC 访问 http://202.100.2.2
Student_PC 访问 http://202.100.2.2
```

如果内网访问公网口失败，但外网能访问，通常是防火墙缺少“内网回流/双向 NAT/NAT hairpin”功能配置。实在来不及，截图外网访问成功，再补一张内网访问 `http://10.60.60.10` 成功作为说明。

---

## 11. 第九步：Branch_Firewall 基础配置

丛台防火墙接口：

| 接口 | Zone | IP |
|---|---|---:|
| e0/4 | Internet | 172.16.2.2/24 |
| e0/1 | Branch | 192.168.30.1/24 |

默认路由：

```text
0.0.0.0/0 -> 172.16.2.1
```

DHCP：

| 地址池 | 绑定接口 | 地址范围 | 网关 | DNS |
|---|---|---|---|---|
| DHCP_Branch | e0/1 | 192.168.30.100-192.168.30.200 | 192.168.30.1 | 8.8.8.8 |

安全策略：

| 策略名 | 源 Zone | 目的 Zone | 服务 | 动作 |
|---|---|---|---|---|
| Branch_to_Internet | Branch | Internet | any | Permit |
| Branch_to_VPN | Branch | VPN | ICMP/any | Permit |
| VPN_to_Branch | VPN | Branch | ICMP/any | Permit |

验收：

```text
Branch_User ping 192.168.30.1
Branch_User ping 202.100.2.2
Branch_User ping 202.100.100.10
```

---

## 12. 第十步：IPSec VPN

PDF 给的预共享密钥：

```text
Hillstone@2026
```

### 12.1 VPN 对端信息

| 项 | 新校区 Core_Firewall01 | 丛台 Branch_Firewall |
|---|---:|---:|
| 公网 IP | 202.100.2.2 | 172.16.2.2 |
| 本地内网 | 192.168.2.0/24、10.10.2.0/24 | 192.168.30.0/24 |
| 对端内网 | 192.168.30.0/24 | 192.168.2.0/24、10.10.2.0/24 |
| 预共享密钥 | Hillstone@2026 | Hillstone@2026 |

### 12.2 推荐参数

如果界面要填 IKE/IPSec 参数，用这套普通参数：

| 项 | 值 |
|---|---|
| IKE 版本 | IKEv1 或 IKEv2，双方一致 |
| 认证方式 | Pre-shared key |
| 加密 | AES-256 或 AES-128 |
| 认证/哈希 | SHA256 或 SHA1 |
| DH Group | Group 14 或 Group 2 |
| IPSec 协议 | ESP |
| PFS | 可关，或双方一致 |

### 12.3 路由

Core_Firewall01 添加：

```text
192.168.30.0/24 -> VPN tunnel
```

Branch_Firewall 添加：

```text
192.168.2.0/24 -> VPN tunnel
10.10.2.0/24 -> VPN tunnel
```

### 12.4 VPN 验收

在 Branch_User 测试：

```text
ping 192.168.2.100
ping 10.10.2.100
```

这里的 `.100` 要换成 Staff_PC、Student_PC 实际 DHCP 获取到的地址。

在防火墙上截图：

```text
show isakmp sa
show ipsec sa
show session
```

验收标准是 Phase 1/Phase 2 都 ACTIVE，有 SPI 值，有 VPN 会话。

---

## 13. 最终截图清单

按这个清单交最稳：

1. PNET 总拓扑图。
2. IP 地址规划表。
3. Switch 的 `show vlan brief`。
4. Switch 的 `show interfaces trunk`。
5. Core_Firewall01 接口列表：e0/1.10、e0/1.20、e0/2、e0/4 都 UP。
6. Core_Firewall01 DHCP 地址池。
7. Staff_PC 自动获取 IP，并 ping `192.168.2.1` 成功。
8. Student_PC 自动获取 IP，并 ping `10.10.2.1` 成功。
9. Staff_PC ping IDC_Server `10.60.60.10` 成功。
10. Student_PC ping Staff_PC 失败。
11. Staff_PC/Student_PC ping Guest_User `202.100.100.10` 成功。
12. Core_Firewall01 SNAT 规则截图。
13. Core_Firewall01 安全策略截图。
14. Guest_User 访问 `http://202.100.2.2` 成功。
15. NAT Server/端口映射规则截图。
16. Branch_User 获取 IP，并 ping 自己网关 `192.168.30.1` 成功。
17. VPN 配置截图。
18. `show isakmp sa` ACTIVE。
19. `show ipsec sa` ACTIVE。
20. Branch_User ping Staff_PC 或 Student_PC 成功。

---

## 14. 做的时候按这个顺序

不要一口气配完再测，容易不知道哪里错。按这个顺序做：

1. 先画完拓扑，确认线接对。
2. 配 Internet Router，确认三个公网段接口 UP。
3. 配 Switch VLAN/Trunk。
4. 配 Core_Firewall01 子接口和网关。
5. 测 Staff/Student/IDC 各自 ping 网关。
6. 配 DHCP，确认终端自动拿地址。
7. 配安全策略，测 Staff 到 IDC、Student 到 Staff。
8. 配 SNAT，测内网 ping Guest_User。
9. 配 NAT Server，测 Guest_User 访问 `http://202.100.2.2`。
10. 配 Branch_Firewall 基础网络。
11. 配 IPSec VPN。
12. 最后按截图清单补齐材料。

如果某一步不通，就停在那一步排错，不要继续往后堆配置。

